# --- Node ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json.*   # partial files left by aborted installs

# --- Build Artifacts ---
dist/
build/
coverage/
*.log
*.log.*
logs/
**/*.tsbuildinfo

# --- Env / Secrets ---
.env
.env.*              # includes .env.local, .env.production, etc.
!.env.example       # keep a template without secrets

# --- Prisma (if used) ---
# Keep migrations in git; ignore local dbs and generated clients if any
prisma/dev.db
prisma/dev.sqlite
prisma/*.db
prisma/*.sqlite
# If you generate to a custom path, ignore it:
# prisma/generated/

# --- Uploads/Runtime storage (if any) ---
uploads/
tmp/
temp/
*.pid
*.seed

# --- Editor / OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo

# --- Docker (if used) ---
docker-compose.override.yml
.docker/
*.pid

# --- PM2 (server logs if kept in repo root) ---
.pids/
pids/
pids-*.log
pm2.log
pm2.pid